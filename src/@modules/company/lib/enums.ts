export const ENUM_PRICING_TYPE = {
  free: 'free',
  paid: 'paid',
};

export const pricingTypeArray = Object.values(ENUM_PRICING_TYPE);

export const ENUM_COURSE_TYPE = {
  LANGUAGE: 'LANGUAGE',
  SKILL: 'SKILL',
};

export const courseTypeArray = Object.values(ENUM_COURSE_TYPE);
